/* ============================================================
   Biz 번역 — Calm UI (2026-09-23)
   눈의 피로를 줄이는 방향: 무채색 중심 + 강조색 하나, 굵기로만 강조,
   본문 16px · 줄간격 1.7 · 한국어 단어 단위 줄바꿈, 넓은 화면은 가운데 읽기 칸.
   설계 원칙: skills/user/edenfarm-calm-ui
   ============================================================ */

:root {
  color-scheme: light;
  --bg: #f5f6f8;
  --surface: #ffffff;
  --surface-2: #eef1f4;
  --hover: rgba(31, 41, 51, .05);
  --line: #e4e7eb;
  --line-strong: #d3d8de;
  --text: #1f2933;
  --text-2: #3e4c59;
  --text-3: #5f6b78;
  --muted: #7b8794;
  --accent: #3a63a0;
  --accent-hover: #335891;
  --accent-text: #ffffff;
  --accent-weak: #eaf0f8;
  --accent-line: #d4dfee;
  --danger: #b4443a;
  --danger-weak: #fbeeec;
  --shadow: 0 1px 2px rgba(31, 41, 51, .05);
  --shadow-lg: 0 12px 40px rgba(31, 41, 51, .14);
  --ring: 0 0 0 3px rgba(58, 99, 160, .22);
  --overlay: rgba(21, 24, 28, .42);

  --font: "Pretendard Variable", Pretendard, -apple-system, BlinkMacSystemFont, "Apple SD Gothic Neo",
          "Malgun Gothic", "Segoe UI", system-ui, sans-serif;
  --fs: 16px;          /* 설정 → 글자 크기 로 바뀐다 */
  --col: 820px;        /* 넓은 화면의 읽기 칸 너비 */
  --gutter: 24px;
  --radius: 14px;
  --sidebar-w: 288px;
}

/* 어두운 테마: 자동(기기 설정) 또는 설정에서 직접 선택 */
:root[data-theme="dark"] {
  color-scheme: dark;
  --bg: #15181c;
  --surface: #1d2126;
  --surface-2: #191c20;
  --hover: rgba(221, 226, 231, .06);
  --line: #2b3036;
  --line-strong: #3a4048;
  --text: #dde2e7;
  --text-2: #bcc4cd;
  --text-3: #9aa4af;
  --muted: #7e8893;
  --accent: #86a8d8;
  --accent-hover: #97b5de;
  --accent-text: #11161c;
  --accent-weak: #223041;
  --accent-line: #2f4058;
  --danger: #e0877e;
  --danger-weak: #2e1f1e;
  --shadow: 0 1px 2px rgba(0, 0, 0, .25);
  --shadow-lg: 0 12px 40px rgba(0, 0, 0, .5);
  --ring: 0 0 0 3px rgba(134, 168, 216, .3);
  --overlay: rgba(0, 0, 0, .55);
}
@media (prefers-color-scheme: dark) {
  :root:not([data-theme="light"]) {
    color-scheme: dark;
    --bg: #15181c;
    --surface: #1d2126;
    --surface-2: #191c20;
    --hover: rgba(221, 226, 231, .06);
    --line: #2b3036;
    --line-strong: #3a4048;
    --text: #dde2e7;
    --text-2: #bcc4cd;
    --text-3: #9aa4af;
    --muted: #7e8893;
    --accent: #86a8d8;
    --accent-hover: #97b5de;
    --accent-text: #11161c;
    --accent-weak: #223041;
    --accent-line: #2f4058;
    --danger: #e0877e;
    --danger-weak: #2e1f1e;
    --shadow: 0 1px 2px rgba(0, 0, 0, .25);
    --shadow-lg: 0 12px 40px rgba(0, 0, 0, .5);
    --ring: 0 0 0 3px rgba(134, 168, 216, .3);
    --overlay: rgba(0, 0, 0, .55);
  }
}

/* 글자 크기 (설정) */
:root[data-size="l"] { --fs: 17.5px; }
:root[data-size="xl"] { --fs: 19px; }

/* ---------- 기본 ---------- */
* { box-sizing: border-box; }

html {
  font-size: var(--fs);
  -webkit-text-size-adjust: 100%;
}
html, body {
  height: 100%;
  margin: 0;
  background: var(--bg);
  color: var(--text);
  font-family: var(--font);
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
  text-rendering: optimizeLegibility;
  font-feature-settings: "ss06";   /* Pretendard: 구분이 쉬운 l·I */
}

/* 한국어는 단어 중간에서 끊지 않는다. 긴 URL·코드만 어디서든 끊는다. */
body { word-break: keep-all; overflow-wrap: anywhere; }

button, input, select, textarea { font: inherit; color: inherit; letter-spacing: inherit; }
button { cursor: pointer; }
[hidden] { display: none !important; }
::selection { background: var(--accent-line); color: var(--text); }

:focus { outline: none; }
:focus-visible { outline: none; box-shadow: var(--ring); border-radius: 8px; }

/* 얇고 조용한 스크롤바 */
* { scrollbar-width: thin; scrollbar-color: var(--line-strong) transparent; }
::-webkit-scrollbar { width: 10px; height: 10px; }
::-webkit-scrollbar-thumb { background: var(--line-strong); border-radius: 10px; border: 3px solid transparent; background-clip: content-box; }
::-webkit-scrollbar-track { background: transparent; }

@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after { animation: none !important; transition: none !important; }
}

#app {
  display: grid;
  grid-template-columns: var(--sidebar-w) 1fr;
  height: 100dvh;
}

/* ---------- 버튼 ---------- */
.primary-btn {
  background: var(--accent);
  color: var(--accent-text);
  border: 1px solid transparent;
  border-radius: 10px;
  padding: .6rem 1.1rem;
  font-weight: 600;
  font-size: .94rem;
  line-height: 1.3;
  transition: background .15s;
}
.primary-btn:hover { background: var(--accent-hover); }
.primary-btn:disabled { opacity: .45; cursor: default; }

.ghost-btn {
  background: var(--surface);
  color: var(--text-2);
  border: 1px solid var(--line-strong);
  border-radius: 10px;
  padding: .5rem .85rem;
  font-size: .88rem;
  font-weight: 500;
  line-height: 1.3;
  transition: background .15s, border-color .15s;
}
.ghost-btn:hover { background: var(--hover); border-color: var(--muted); color: var(--text); }

.danger-btn {
  background: transparent;
  color: var(--danger);
  border: 1px solid currentColor;
  border-radius: 10px;
  padding: .5rem .85rem;
  font-size: .88rem;
  font-weight: 500;
}
.danger-btn:hover { background: var(--danger-weak); }

.icon-btn {
  background: transparent;
  border: 0;
  color: var(--text-3);
  font-size: 1.15rem;
  line-height: 1;
  padding: .45rem .6rem;
  border-radius: 8px;
}
.icon-btn:hover { background: var(--hover); color: var(--text); }
.only-mobile { display: none; }

/* ---------- 사이드바 (밝은 무채색 — 어두운 막대가 눈을 지치게 한다) ---------- */
#sidebar {
  background: var(--surface-2);
  color: var(--text);
  border-right: 1px solid var(--line);
  display: flex;
  flex-direction: column;
  padding: 16px 12px calc(12px + env(safe-area-inset-bottom));
  padding-top: calc(16px + env(safe-area-inset-top));
  min-height: 0;
}
.sidebar-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin: 0 4px 14px;
}
.brand {
  display: flex;
  align-items: center;
  gap: 9px;
  font-weight: 700;
  font-size: 1.02rem;
  color: var(--text);
}
.brand img { width: 26px; height: 26px; border-radius: 7px; }

#btn-new-room {
  width: 100%;
  background: var(--surface);
  color: var(--text);
  border: 1px solid var(--line-strong);
  box-shadow: var(--shadow);
  font-weight: 600;
}
#btn-new-room:hover { background: var(--surface); border-color: var(--muted); }

#room-list {
  list-style: none;
  margin: 14px 0 0;
  padding: 0;
  overflow-y: auto;
  flex: 1;
  min-height: 0;
}
#room-list li {
  display: flex;
  align-items: center;
  gap: 11px;
  padding: 10px 10px;
  border-radius: 11px;
  cursor: pointer;
  margin-bottom: 2px;
  border: 1px solid transparent;
  transition: background .12s;
}
#room-list li:hover { background: var(--hover); }
#room-list li.active {
  background: var(--surface);
  border-color: var(--line);
  box-shadow: var(--shadow);
}
.room-badge {
  flex: none;
  width: 34px;
  height: 34px;
  border-radius: 9px;
  background: var(--surface);
  border: 1px solid var(--line);
  color: var(--text-3);
  display: grid;
  place-items: center;
  font-size: .72rem;
  font-weight: 700;
  letter-spacing: .02em;
}
#room-list li.active .room-badge {
  background: var(--accent-weak);
  border-color: var(--accent-line);
  color: var(--accent);
}
.room-info { min-width: 0; flex: 1; }
.room-info strong {
  display: block;
  font-weight: 600;
  font-size: .94rem;
  color: var(--text);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
.room-info span {
  display: block;
  font-size: .8rem;
  color: var(--text-3);
  margin-top: 1px;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.sidebar-foot { padding-top: 10px; border-top: 1px solid var(--line); }
.sidebar-foot .ghost-btn { width: 100%; background: transparent; }
.sidebar-foot .ghost-btn:hover { background: var(--hover); }

/* ---------- 채팅 ---------- */
#chat { display: flex; flex-direction: column; min-width: 0; min-height: 0; background: var(--bg); }

/* 넓은 화면에서도 글이 가운데 한 칸에 모이도록 좌우 여백을 계산한다 */
#chat-head,
#messages,
#composer {
  padding-left: max(var(--gutter), calc((100% - var(--col)) / 2));
  padding-right: max(var(--gutter), calc((100% - var(--col)) / 2));
}

#chat-head {
  display: flex;
  align-items: center;
  gap: 12px;
  min-height: 62px;
  padding-top: calc(10px + env(safe-area-inset-top));
  padding-bottom: 10px;
  background: var(--surface);
  border-bottom: 1px solid var(--line);
}
.chat-title { flex: 1; min-width: 0; }
.chat-title h1 {
  font-size: 1.06rem;
  font-weight: 700;
  margin: 0;
  color: var(--text);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
.chat-title p { margin: 1px 0 0; font-size: .8rem; color: var(--text-3); }

#messages {
  flex: 1;
  min-height: 0;
  overflow-y: auto;
  padding-top: 28px;
  padding-bottom: 28px;
  display: flex;
  flex-direction: column;
  gap: 26px;
}

.empty {
  margin: auto;
  text-align: center;
  color: var(--text-3);
  line-height: 1.75;
  max-width: 460px;
  font-size: .94rem;
}
.empty strong { display: block; color: var(--text); font-size: 1.06rem; font-weight: 700; margin-bottom: 6px; }
.empty p { margin: 8px 0 0; }

/* 메시지 한 덩어리: [원문] → [번역 말풍선] → [참고] → [도구] */
.msg { display: flex; flex-direction: column; max-width: min(88%, 720px); }
.msg.me { align-self: flex-end; align-items: flex-end; }
.msg.partner { align-self: flex-start; align-items: flex-start; }

.msg-source {
  font-size: .9rem;
  line-height: 1.65;
  color: var(--text-3);
  margin: 0 4px 6px;
  white-space: pre-wrap;
}
.msg.me .msg-source { text-align: left; }

.bubble {
  font-size: 1rem;
  line-height: 1.72;
  border-radius: var(--radius);
  padding: .75rem 1rem;
  white-space: pre-wrap;
  border: 1px solid var(--line);
  background: var(--surface);
  color: var(--text);
  box-shadow: var(--shadow);
}
.msg.me .bubble {
  background: var(--accent-weak);
  border-color: var(--accent-line);
  border-bottom-right-radius: 5px;
}
.msg.partner .bubble { border-bottom-left-radius: 5px; }
.msg.error .bubble {
  background: var(--danger-weak);
  border: 1px solid var(--danger);
  color: var(--danger);
  box-shadow: none;
  font-size: .92rem;
}
.msg.pending .bubble { color: var(--text-3); box-shadow: none; }

.msg-note {
  font-size: .86rem;
  line-height: 1.65;
  color: var(--text-2);
  margin: 8px 4px 0;
  padding: .45rem .7rem;
  border-left: 2px solid var(--line-strong);
  background: transparent;
  max-width: 100%;
}
.msg-note::before {
  content: "참고";
  font-weight: 700;
  color: var(--text-3);
  margin-right: .5em;
}

.msg-tools {
  display: flex;
  flex-wrap: wrap;
  gap: 2px;
  margin: 6px 0 0;
  align-items: center;
}
.msg-tools button {
  background: transparent;
  border: 0;
  color: var(--muted);
  font-size: .8rem;
  font-weight: 500;
  padding: 4px 8px;
  border-radius: 7px;
}
.msg-tools button:hover { background: var(--hover); color: var(--text); }
.msg-tools .dir { font-size: .76rem; color: var(--muted); padding: 0 6px; }

.dots::after { content: " …"; animation: blink 1.4s infinite steps(3); }
@keyframes blink { 0% { opacity: .25; } 50% { opacity: 1; } 100% { opacity: .25; } }

/* ---------- 입력칸 ---------- */
#composer {
  display: flex;
  flex-direction: column;
  gap: 10px;
  padding-top: 12px;
  padding-bottom: calc(14px + env(safe-area-inset-bottom));
  background: var(--surface);
  border-top: 1px solid var(--line);
}
.composer-row {
  display: flex;
  gap: 8px;
  align-items: flex-end;
  background: var(--bg);
  border: 1px solid var(--line-strong);
  border-radius: 14px;
  padding: 6px;
  transition: border-color .15s, box-shadow .15s;
}
.composer-row:focus-within { border-color: var(--accent); box-shadow: var(--ring); }

#input {
  flex: 1;
  resize: none;
  max-height: 180px;
  border: 0;
  background: transparent;
  padding: .55rem .4rem;
  line-height: 1.6;
  font-size: max(16px, 1rem); /* iOS 자동 확대 방지 */
  color: var(--text);
}
#input::placeholder { color: var(--muted); }
#input:focus, #input:focus-visible { outline: none; box-shadow: none; }

.attach-btn {
  display: grid;
  place-items: center;
  padding: 8px;
  border-radius: 10px;
  color: var(--text-3);
  border: 0;
  align-self: flex-end;
}
.attach-btn:hover { background: var(--hover); color: var(--text); }
#btn-send { align-self: flex-end; padding: .58rem 1.2rem; }

/* ---------- 다이얼로그 ---------- */
dialog {
  border: 1px solid var(--line);
  border-radius: 16px;
  padding: 0;
  background: var(--surface);
  color: var(--text);
  width: min(500px, calc(100vw - 24px));
  max-height: calc(100dvh - 24px);
  box-shadow: var(--shadow-lg);
}
dialog::backdrop { background: var(--overlay); }
.dlg-body { padding: 22px; display: flex; flex-direction: column; gap: 14px; }
.dlg-body h2 { margin: 0 0 2px; font-size: 1.12rem; font-weight: 700; }
.dlg-body h3 { margin: 8px 0 0; font-size: .92rem; font-weight: 700; color: var(--text); }
.dlg-body label {
  display: flex;
  flex-direction: column;
  gap: 6px;
  font-size: .84rem;
  font-weight: 600;
  color: var(--text-2);
}
.dlg-body input, .dlg-body select, .dlg-body textarea {
  border: 1px solid var(--line-strong);
  border-radius: 10px;
  padding: .6rem .75rem;
  background: var(--surface);
  color: var(--text);
  font-size: max(16px, .95rem);
  font-weight: 400;
  line-height: 1.5;
}
.dlg-body input:focus, .dlg-body select:focus, .dlg-body textarea:focus {
  border-color: var(--accent);
  box-shadow: var(--ring);
  border-radius: 10px;
}
.dlg-body textarea { resize: vertical; }
.dlg-actions { display: flex; gap: 8px; align-items: center; margin-top: 6px; }
.spacer { flex: 1; }
.hint { font-size: .8rem; color: var(--text-3); margin: 0; line-height: 1.6; font-weight: 400; }
.field-row { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; }
.backup { display: flex; flex-wrap: wrap; gap: 8px; }
.backup h3, .backup .hint { flex-basis: 100%; }
.only-ios { display: none; }
.is-ios .only-ios { display: block; }

#toast {
  position: fixed;
  left: 50%;
  bottom: calc(96px + env(safe-area-inset-bottom));
  transform: translateX(-50%);
  background: #2a3038;
  color: #eef1f4;
  padding: .6rem 1.1rem;
  border-radius: 999px;
  font-size: .86rem;
  z-index: 50;
  max-width: calc(100vw - 32px);
  box-shadow: var(--shadow-lg);
}

/* ---------- 모바일 ---------- */
@media (max-width: 760px) {
  :root { --gutter: 14px; }
  #app { grid-template-columns: 1fr; }
  .only-mobile { display: inline-block; }
  #sidebar {
    position: fixed;
    inset: 0 auto 0 0;
    width: min(86vw, 320px);
    z-index: 30;
    transform: translateX(-100%);
    transition: transform .22s ease;
    box-shadow: var(--shadow-lg);
  }
  #app.sidebar-open #sidebar { transform: none; }
  #backdrop { position: fixed; inset: 0; background: var(--overlay); z-index: 20; }
  #chat-head { min-height: 56px; gap: 6px; }
  #messages { padding-top: 20px; padding-bottom: 20px; gap: 22px; }
  .msg { max-width: 92%; }
  #btn-room-settings { padding: .42rem .7rem; font-size: .82rem; }
  .field-row { grid-template-columns: 1fr; }
}

/* ---------- 로그인 / 회원가입 ---------- */
.auth-screen {
  min-height: 100dvh;
  display: grid;
  place-items: center;
  padding: 24px 16px calc(24px + env(safe-area-inset-bottom));
  background: var(--bg);
}
.auth-card {
  width: min(420px, 100%);
  background: var(--surface);
  border: 1px solid var(--line);
  border-radius: 16px;
  padding: 28px 24px;
  box-shadow: var(--shadow);
}
.auth-brand {
  display: flex;
  align-items: center;
  gap: 9px;
  font-weight: 700;
  font-size: 1.02rem;
  color: var(--text);
  margin-bottom: 16px;
}
.auth-card h1 { margin: 0 0 6px; font-size: 1.35rem; font-weight: 700; }
.auth-card form { margin-top: 18px; }
.auth-card label {
  display: block;
  margin-bottom: 14px;
  font-size: .84rem;
  font-weight: 600;
  color: var(--text-2);
}
.auth-card input {
  display: block;
  width: 100%;
  margin-top: 6px;
  padding: .7rem .8rem;
  border: 1px solid var(--line-strong);
  border-radius: 10px;
  background: var(--surface);
  font-weight: 400;
  font-size: max(16px, 1rem);
  color: var(--text);
}
.auth-card input:focus { border-color: var(--accent); box-shadow: var(--ring); border-radius: 10px; }
.auth-submit { width: 100%; padding: .75rem; margin-top: 4px; font-size: 1rem; }
.auth-error {
  margin: 0 0 14px;
  padding: .6rem .75rem;
  border-radius: 10px;
  background: var(--danger-weak);
  border: 1px solid var(--danger);
  color: var(--danger);
  font-size: .86rem;
}
.link-btn {
  display: block;
  width: 100%;
  margin-top: 14px;
  padding: 8px;
  border: 0;
  background: none;
  color: var(--accent);
  font-size: .88rem;
  font-weight: 600;
}
.link-btn:hover { text-decoration: underline; }
.auth-foot { margin-top: 18px; padding-top: 14px; border-top: 1px solid var(--line); }

/* ---------- 설정: 계정 · 초대 ---------- */
.account-box {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 10px;
  padding: 12px 14px;
  border: 1px solid var(--line);
  border-radius: 12px;
  background: var(--bg);
}
.account-box strong { display: block; font-weight: 600; word-break: break-all; }
.role-tag {
  display: inline-block;
  margin-top: 4px;
  padding: 1px 8px;
  border-radius: 999px;
  background: var(--accent-weak);
  border: 1px solid var(--accent-line);
  color: var(--accent);
  font-size: .74rem;
  font-weight: 600;
}
.admin-box { margin-top: 6px; padding-top: 14px; border-top: 1px solid var(--line); }
.admin-box h3 { margin: 0 0 6px; }
.invite-row { display: flex; align-items: flex-end; gap: 10px; margin-top: 10px; }
.invite-count { flex: 0 0 96px; }
.invite-count input { width: 100%; }
.invite-result {
  margin: 12px 0 0;
  padding: .7rem .8rem;
  border: 1px solid var(--line);
  border-radius: 10px;
  background: var(--bg);
  font-family: ui-monospace, SFMono-Regular, Consolas, monospace;
  font-size: .86rem;
  white-space: pre-wrap;
  word-break: break-all;
  user-select: all;
}

/* ---------- 이미지 첨부 ---------- */
.attachments { display: flex; flex-wrap: wrap; gap: 8px; align-items: center; }
.attach-item {
  position: relative;
  width: 72px;
  height: 72px;
  border-radius: 10px;
  overflow: hidden;
  border: 1px solid var(--line-strong);
  background: var(--surface);
  flex: none;
}
.attach-item img { width: 100%; height: 100%; object-fit: cover; display: block; }
.attach-remove {
  position: absolute;
  top: 3px;
  right: 3px;
  width: 22px;
  height: 22px;
  border: 0;
  border-radius: 50%;
  background: rgba(31, 41, 51, .72);
  color: #fff;
  font-size: 12px;
  padding: 0;
  display: grid;
  place-items: center;
}
.attach-hint { font-size: .8rem; color: var(--text-3); line-height: 1.5; }

.msg-images { display: flex; flex-wrap: wrap; gap: 6px; margin: 0 4px 8px; max-width: 100%; }
.msg.me .msg-images { justify-content: flex-end; }
.msg-images img,
.msg-images .thumb-missing {
  height: 128px;
  width: auto;
  max-width: min(240px, 60vw);
  object-fit: contain;
  object-position: top;
  border-radius: 10px;
  border: 1px solid var(--line);
  background: #fff;
  display: block;
  cursor: zoom-in;
}
.msg-images .thumb-missing {
  width: 128px;
  background: var(--surface-2);
  display: grid;
  place-items: center;
  font-size: .76rem;
  color: var(--muted);
  text-align: center;
  padding: 6px;
  cursor: default;
}
.msg-ocr {
  display: inline-block;
  margin: 0 4px 3px;
  font-size: .74rem;
  font-weight: 600;
  color: var(--muted);
  letter-spacing: .01em;
}

.dropzone {
  position: fixed;
  inset: 0;
  z-index: 60;
  display: grid;
  place-items: center;
  background: var(--overlay);
}
.dropzone-card {
  display: grid;
  gap: 6px;
  justify-items: center;
  padding: 28px 36px;
  border-radius: 16px;
  border: 2px dashed var(--accent);
  background: var(--surface);
  color: var(--text);
  text-align: center;
  pointer-events: none;
  box-shadow: var(--shadow-lg);
}
.dropzone-card strong { font-size: 1.06rem; }
.dropzone-card span { font-size: .86rem; color: var(--text-3); }

.image-dialog { width: auto; max-width: calc(100vw - 24px); background: transparent; border: 0; box-shadow: none; }
.image-dialog img {
  display: block;
  max-width: calc(100vw - 24px);
  max-height: calc(100dvh - 24px);
  border-radius: 12px;
  background: #fff;
}
